# Database
POSTGRES_USER=jesse_user
POSTGRES_PASSWORD=password
POSTGRES_DB=jesse_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Backend
# Jesse requires these usually, or config.py handles them. 
# We'll map them in docker-compose usually, but good to have here for reference or local dev without docker.
